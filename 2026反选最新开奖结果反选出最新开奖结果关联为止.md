最核心方案反选最新开奖结果，最好反选出最新开奖结果号码关连为止，



<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>彩票走势图分析工具 - 手指拖拽翻页版</title>

<style>
  body {
    font-family: Arial, sans-serif;
    padding: 10px;
    overscroll-behavior: none;
    max-width: 1000px;
    margin: 0 auto;
    transform: scale(0.95);
    transform-origin: top center;
    width: 100%;
  }

  .pageGroup {
    display: flex;
    flex-direction: column;
    gap: 25px;
  }

  .iframeContainer {
    position: relative;
    width: 100%;
    max-width: 700px;
    height: 2800px;
    border: 1px solid #ccc;
    overflow: hidden;
    touch-action: none;
    margin: 0 auto;
  }

  iframe {
    width: 100%;
    height: 100%;
    border: none;
    display: block;
  }

  .sizeDisplay {
    position: absolute;
    top: 5px;
    left: 5px;
    background: rgba(0,0,0,0.5);
    color: #fff;
    padding: 2px 5px;
    font-size: 12px;
    border-radius: 3px;
    z-index: 10;
  }

  .nav {
    text-align: center;
    margin: 20px 0;
  }

  .nav button {
    padding: 8px 16px;
    margin: 0 10px;
    font-size: 16px;
    border: none;
    background: #007bff;
    color: #fff;
    border-radius: 6px;
  }

  .nav button:disabled {
    background: #aaa;
  }

  .pageTitle {
    text-align: center;
    margin: 20px 0;
    font-size: 24px;
    color: #333;
    font-weight: bold;
  }
</style>

</head>
<body>

<!-- 这里显示每页不同的标题 -->
<div id="pageTitle" class="pageTitle"></div>

<div class="nav">
  <button id="prevBtn">上一页</button>
  <span id="pageInfo">1 / 1</span>
  <button id="nextBtn">下一页</button>
</div>

<div id="pageArea"></div>

<script>
const iframeList = [
  "4UFOOb.php",
  "OFO888888888888882026.php",
  "2fxhqqqqqqqqqqqqqq.php",   
  "bijiU888888888888888.php",
  "2hqqqqqqqqqqqqqqq.php",   
                                             
  "1UFOOa.php",
  "OFO888888888888882026.php",
  "bijiU888888888888888.php",
  "2fxhqqqqqqqqqqqqqq.php",
  "2hqqqqqqqqqqqqqqq.php",   
                                                  
  "2fxhqqqqqqqqqqqqqq.php",   
  "6UFOOc.php",
  "OFO888888888888882026.php",
  "bijiU888888888888888.php",           
  "2hqqqqqqqqqqqqqqq.php",                                                                                            
  "2fxhqqqqqqqqqqqqqq.php",  
  "2OK2d.php",
  "OFO888888888888882026.php",
  "bijiU888888888888888.php",
  "sn99999999999sn.php", 
  "2hqqqqqqqqqqqqqqq.php",                  
            
  "2fxhqqqqqqqqqqqqqq.php",   
  "3Ok3e.php",
  "OFO888888888888882026.php",
  "bijiU888888888888888.php",
  "2hqqqqqqqqqqqqqqq.php",   
];

// 每页显示多少个 iframe（可以修改这个值）
let pageSize = 5; // 修改这里可以改变每页显示数量

// 计算总页数
const totalPages = Math.ceil(iframeList.length / pageSize);
let currentPage = 0;

// 预定义的标题文字（可以根据需要修改）
const predefinedTitles = [
  "第二部分：反选最新开开奖结果核心方案反选到关连上期码为止",
  "第一部分：随机，自动反选专用，1-35自由随一",
  "第三部分：最新开奖结果关联找齐，无重胆",
  "第四部分：重胆版，最新开奖结果关联找齐",
  "第五部分：重胆版，反选最新开奖结果随机"
];

// 根据总页数动态生成标题数组
function generatePageTitles() {
  let titles = [];
  
  for (let i = 0; i < totalPages; i++) {
    // 如果有预定义的标题就用预定义的，否则生成默认标题
    if (i < predefinedTitles.length) {
      titles.push(predefinedTitles[i]);
    } else {
      titles.push(`第${i + 1}部分：走势图分析`);
    }
  }
  
  return titles;
}

const pageArea = document.getElementById("pageArea");
const prevBtn = document.getElementById("prevBtn");
const nextBtn = document.getElementById("nextBtn");
const pageInfo = document.getElementById("pageInfo");
const titleElement = document.getElementById("pageTitle");

// 更新页面
function updatePage() {
  const start = currentPage * pageSize;
  const end = start + pageSize;
  const items = iframeList.slice(start, end);
  
  // 生成当前页对应的标题数组
  const pageTitles = generatePageTitles();
  
  // 更新标题
  const titleText = pageTitles[currentPage];
  titleElement.textContent = titleText;
  
  // 更新页面内容
  pageArea.innerHTML = `
    <div class="pageGroup">
      ${items.map(src => `
        <div class='iframeContainer'>
          <div class='sizeDisplay'></div>
          <iframe src="${src}"></iframe>
        </div>
      `).join('')}
    </div>
  `;

  // 更新页码信息
  pageInfo.textContent = `${currentPage + 1} / ${totalPages}`;
  prevBtn.disabled = currentPage === 0;
  nextBtn.disabled = currentPage === totalPages - 1;
  
  // 更新浏览器标签页标题
  document.title = `${titleText} - 彩票走势图分析工具`;

  initResize();
  
  // 添加iframe加载错误处理
  document.querySelectorAll('iframe').forEach(iframe => {
    iframe.addEventListener('load', function() {
      console.log("iframe loaded:", this.src);
    });
    iframe.addEventListener('error', function() {
      console.error("Failed to load iframe:", this.src);
    });
  });
}

// 重新初始化页面（当修改pageSize后调用）
function reinitializePage(newPageSize) {
  pageSize = newPageSize;
  currentPage = 0;
  updatePage();
}

// 翻页按钮事件
prevBtn.onclick = () => {
  if (currentPage > 0) {
    currentPage--;
    updatePage();
  }
};

nextBtn.onclick = () => {
  if (currentPage < totalPages - 1) {
    currentPage++;
    updatePage();
  }
};

// 保留你原来的拖拽缩放功能
function initResize() {
  const containers = document.querySelectorAll('.iframeContainer');
  const MIN_WIDTH = 100;
  const MAX_WIDTH = 700;

  containers.forEach(container => {
      const display = container.querySelector('.sizeDisplay');
      let startX, startWidth, startHeight;

      function updateDisplay() {
          display.textContent = `${container.offsetWidth}px × ${container.offsetHeight}px`;
      }
      updateDisplay();

      container.addEventListener('touchstart', function(e) {
          e.preventDefault();
          const t = e.touches[0];
          startX = t.clientX;
          startWidth = container.offsetWidth;
          startHeight = container.offsetHeight;
      }, { passive: false });

      container.addEventListener('touchmove', function(e) {
          e.preventDefault();
          const t = e.touches[0];
          let dx = t.clientX - startX;

          let newWidth = Math.min(Math.max(startWidth + dx, MIN_WIDTH), MAX_WIDTH);
          container.style.width = newWidth + 'px';

          let scale = newWidth / startWidth;
          let newHeight = Math.max(100, startHeight * scale);
          container.style.height = newHeight + 'px';

          updateDisplay();
      }, { passive: false });

      container.addEventListener('touchend', function() {
          updateDisplay();
      });
  });
}

// 初始化页面
updatePage();

// 添加键盘快捷键支持
document.addEventListener('keydown', function(e) {
  if (e.key === 'ArrowLeft') {
    prevBtn.onclick();
  } else if (e.key === 'ArrowRight') {
    nextBtn.onclick();
  }
});

// 添加触摸滑动手势支持
let touchStartX = 0;
let touchEndX = 0;

document.addEventListener('touchstart', function(e) {
  touchStartX = e.changedTouches[0].screenX;
});

document.addEventListener('touchend', function(e) {
  touchEndX = e.changedTouches[0].screenX;
  handleSwipe();
});

function handleSwipe() {
  const minSwipeDistance = 50;
  const distance = touchStartX - touchEndX;
  
  if (Math.abs(distance) < minSwipeDistance) return;
  
  if (distance > 0 && currentPage < totalPages - 1) {
    // 向左滑动，下一页
    nextBtn.onclick();
  } else if (distance < 0 && currentPage > 0) {
    // 向右滑动，上一页
    prevBtn.onclick();
  }
}

// 这里添加一个简单的控制台提示
console.log(`当前每页显示 ${pageSize} 个iframe，共 ${totalPages} 页`);
console.log('要修改每页显示数量，请调用 reinitializePage(新数量)');
console.log('例如：reinitializePage(3) 将每页显示3个iframe');
console.log('当前文件列表：', iframeList);
</script>

</body>
</html>
